name: Release spread-plus

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.arch }}
    runs-on: ubuntu-latest

    strategy:
      matrix:
        include:
          - arch: amd64
            goarch: amd64
          - arch: arm64
            goarch: arm64

    steps:
      - name: Checkout release
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}

      - name: Install Go
        run: |
          sudo snap install go --classic

      - name: Build spread-plus
        env:
          GOARCH: ${{ matrix.goarch }}
          GOOS: linux
        run: |
          VERSION="${{ github.event.release.tag_name }}"

          go build \
            -ldflags "-X main.version=${VERSION}" \
            -o spread-plus \
            ./cmd/spread

      - name: Package
        run: |
          tar -czf "spread-plus-${{ matrix.arch }}.tar.gz" spread-plus

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: spread-plus-${{ matrix.arch }}
          path: spread-plus-${{ matrix.arch }}.tar.gz

  build-snap:
    name: Build snap ${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}

    strategy:
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-latest
          - arch: arm64
            runner: ubuntu-24.04-arm

    steps:
      - name: Checkout release
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}

      - name: Build snap
        id: snapcraft
        uses: snapcore/action-build@v1

      - name: Rename snap
        env:
          BUILT_SNAP: ${{ steps.snapcraft.outputs.snap }}
        run: mv "${BUILT_SNAP}" "spread-plus-${{ matrix.arch }}.snap"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: spread-plus-snap-${{ matrix.arch }}
          path: spread-plus-${{ matrix.arch }}.snap

  release:
    name: Publish release assets
    needs: [build, build-snap]
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Generate checksums
        working-directory: dist
        run: |
          sha256sum \
            spread-plus-amd64.tar.gz \
            spread-plus-arm64.tar.gz \
            spread-plus-amd64.snap \
            spread-plus-arm64.snap \
            > SHA256SUMS

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/spread-plus-amd64.tar.gz
            dist/spread-plus-arm64.tar.gz
            dist/spread-plus-amd64.snap
            dist/spread-plus-arm64.snap
            dist/SHA256SUMS